version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: cd installer/target && make rbp1
      - run: mkdir -p /tmp/artifacts
      - run: pwd
      - run: find -name 'OSMC_TGT_rbp1_*.img.gz'
      - store_artifacts:
          path: /tmp/artifacts
workflows:
  version: 2
  build_and_test:
    jobs:
      - build:
          filters:
            branches:
              only: /circle-.*/
